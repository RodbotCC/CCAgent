# Close Exports (raw)

Run audit logs + raw Close exports (calls, emails, sms, threads, meetings, whatsapp). Written by `export_close_conversations.py`. Required API key (Close).

**Output convention:** date-stamped markdown — `YYYY-MM-DD.md` for daily snapshots, `YYYY-MM-DD_HH-MM.md` for sub-daily.
