# Endpoint Window Alignment

Logs for re-aligning a single endpoint's raw export to a fixed time window. Written by `align_endpoint_window.py`. Required API key (Close).

**Output convention:** date-stamped markdown — `YYYY-MM-DD.md` for daily snapshots, `YYYY-MM-DD_HH-MM.md` for sub-daily.
