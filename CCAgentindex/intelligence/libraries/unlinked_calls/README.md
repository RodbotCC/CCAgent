# Unlinked Call Library

Recorded calls that have no lead_id in Close. Written by `build_unlinked_call_library.py`. Read when reconciling unattributed phone activity.

**Output convention:** date-stamped markdown — `YYYY-MM-DD.md` for daily snapshots, `YYYY-MM-DD_HH-MM.md` for sub-daily.
