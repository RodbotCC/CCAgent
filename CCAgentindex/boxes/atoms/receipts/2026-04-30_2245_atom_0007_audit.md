# ATOM-2026-04-30-0007 — Audit Receipt

**Atom:** ATOM-2026-04-30-0007
**Title:** Audit `CCAgentindex/commitments/` — decide collapse target (likely already retired)
**Parent PROB:** PROB-2026-04-28-016 (bedrock reconciliation)
**Claimed by:** claude_cowork_session_2026-04-30
**Claimed at:** 2026-04-30T22:42:00Z
**Completed at:** 2026-04-30T22:45:00Z
**Effort:** ~3 min (well under 15min estimate — first bedrock audit shipped)

---

## Findings

### Directory state

- **`CCAgentindex/commitments/` exists** but contains a single file: `.gitkeep` (0 bytes).
- File count: 1. Disk size: 0 bytes. Last touched: Apr 26 02:53.
- The `.gitkeep` is the standard placeholder that preserves an empty directory's existence in git. It carries no content.

### Loader / index reference

- **NOT referenced in `CCAgentindex/indexes/index.json`** — clean. The loader does not enumerate this directory.

### Cross-ledger references

- `LEDGERS/FILE_DIRECTORY_LEDGER.md` line 166: `| CCAgentindex/commitments/ | 1 (.gitkeep) | legacy / stub | Retired domain |`
- `LEDGERS/FILE_DIRECTORY_LEDGER.md` line 383: grouped with `threads/`, `projects/`, `knowledge/`, `tables/`, `annotations/`, `charts/` as "legacy / stub — `.gitkeep` only"
- `LEDGERS/FILE_DIRECTORY_LEDGER.json` line 244: `{"path": "CCAgentindex/commitments", "status": ["legacy", "stub"], "role": "Retired domain — .gitkeep only"}`
- Already documented as retired in FDL.
- Covered by `DEPR-2026-04-30-001` (Apr 2026 great-trim retired bedrock domains).

## Decision

**Collapse target: archive (final removal).** The directory is functionally empty, already documented as retired in FDL, already captured in the Deprecation Ledger. The `.gitkeep` is not load-bearing — it just preserves the directory shape in git, but since the directory is being archived entirely, that shape no longer matters.

**Recommended action:** when `ATOM-2026-04-30-0023` (update `indexes/index.json`) runs, no change needed there — already not referenced. When `ATOM-2026-04-30-0024` (update `FILE_DIRECTORY_LEDGER.md`) runs, **drop the row** for `CCAgentindex/commitments/` (currently FDL line 166 + JSON line 244 + the grouped line 383 — keep `threads/`, `projects/`, `knowledge/`, `tables/`, `annotations/`, `charts/` rows pending their own audit-atom completions). When `ATOM-2026-04-30-0026` (update `CLAUDE.md`) runs, ensure surviving-domains list at §1 doesn't accidentally re-introduce `commitments`.

**Filesystem cleanup:** `rmdir CCAgentindex/commitments/` is safe after the FDL row drop. The `.gitkeep` removal is trivial. This filesystem-removal step is appropriate as part of `ATOM-0023` or as a tiny housekeeping atom — not in scope for this audit atom.

## Anomalies surfaced (non-blocking)

1. **Six other "stub-only" dirs follow the same pattern:** `threads/`, `projects/`, `knowledge/`, `tables/`, `annotations/`, `charts/`. Each has its own audit atom in PROB-016 (`0019`, `0013`, `0011`, `0018`, `0004`, `0006` respectively). Once all six complete, they collapse together via the propagation atoms.

## Verdict

**Acceptance criterion satisfied.** Documented disposition: directory is functionally empty (`.gitkeep` only); already retired per DEPR-2026-04-30-001; collapse target = archive; no migration needed (no content to move).

Receipt complete.
