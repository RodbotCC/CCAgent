# {Directory Name} — Orientation

> Lightweight "you are here" file for any directory that doesn't warrant a full Box but still needs a memory anchor.
>
> If a directory grows enough to need ledgers, source-of-truth rules, and a Done Gate, promote it to a full Box using `BOX_LEDGER_TEMPLATE.md`.

Last updated: YYYY-MM-DD
Path: `{relative path from project root}`
Owner: {who maintains this directory}

---

## What lives here

One paragraph. What's this folder for? What kinds of files belong here? What kinds don't?

---

## Files that matter

| File | Purpose |
|---|---|
| `{file}` | {what it does} |

If the directory is mostly storage (e.g. raw exports, generated images), say so and link to whichever script or process produces the contents.

---

## Read order

If an agent walks into this directory and needs to do meaningful work, what should they read first?

1. {file or external ref}
2. {file or external ref}
3. {file or external ref}

---

## Local rules

What's special about working inside this directory?

- {rule}
- {rule}

Common rules to consider:

- "Read-only — generated by `{script}`, do not hand-edit."
- "Do not commit — covered by `.gitignore`."
- "Touch only via {orchestrator / inbox skill / specific tool}."

---

## Promotion to a full Box

Promote this directory to a full Box (using `BOX_LEDGER_TEMPLATE.md`) when any of these become true:

- the directory owns canonical truth that other parts of the system depend on
- it has its own source-of-truth rules
- it has its own forbidden actions
- it has its own Done Gate beyond the global one
- it has more than one ledger applying locally

---

## Links

- Global Ledger: `../LEDGERS/GLOBAL_LEDGER.md`
- Box template (if promoting): `../LEDGERS/LOCAL_TEMPLATE/BOX_LEDGER_TEMPLATE.md`
